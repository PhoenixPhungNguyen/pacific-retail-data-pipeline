CREATE OR REPLACE STREAM customer_change_stream ON TABLE CUSTOMER
APPEND_ONLY = TRUE;

CREATE OR REPLACE STREAM product_change_stream ON TABLE PRODUCT
APPEND_ONLY = TRUE;

CREATE OR REPLACE STREAM orders_change_stream ON TABLE ORDERS
APPEND_ONLY = TRUE;